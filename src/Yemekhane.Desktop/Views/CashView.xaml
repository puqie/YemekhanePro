<UserControl x:Class="Yemekhane.Desktop.Views.CashView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Language="tr-TR" Background="#F4F5F7" FontFamily="Segoe UI" FontSize="12">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <Style x:Key="Panel" TargetType="Border"><Setter Property="Background" Value="White"/><Setter Property="BorderBrush" Value="#E1E5E9"/><Setter Property="BorderThickness" Value="1"/><Setter Property="CornerRadius" Value="7"/></Style>
        <Style x:Key="Action" TargetType="Button"><Setter Property="Padding" Value="11,6"/><Setter Property="Margin" Value="0,0,7,0"/><Setter Property="Background" Value="White"/><Setter Property="BorderBrush" Value="#D7DDE2"/><Setter Property="Cursor" Value="Hand"/></Style>
        <Style x:Key="Field" TargetType="Control"><Setter Property="Height" Value="30"/><Setter Property="Margin" Value="0,0,7,7"/></Style>
        <Style x:Key="Label" TargetType="TextBlock"><Setter Property="Foreground" Value="#65717E"/><Setter Property="Margin" Value="0,7,0,3"/></Style>
    </UserControl.Resources>
    <Grid Margin="22,18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel><TextBlock Text="Kasa" FontSize="22" FontWeight="SemiBold" Foreground="#17202A"/><TextBlock Text="Gelir, günlük kasa ve işlem denetimi • Europe/Istanbul" Foreground="#65717E" Margin="0,4,0,0"/></StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="#FFF3E5" CornerRadius="12" Padding="9,4" Margin="0,0,10,0" Visibility="{Binding IsOffline, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="Çevrimdışı" Foreground="#8A681D"/></Border>
                <Button Style="{StaticResource Action}" Content="Rapor Merkezine Aktar" Command="{Binding OpenReportsCommand}" IsEnabled="{Binding IsExportAvailable}" ToolTip="Günlük kasa ve gelir raporlarını aç" AutomationProperties.Name="Rapor merkezini aç"/>
                <Button Style="{StaticResource Action}" Content="Gelir Ekle" Command="{Binding OpenAddCommand}" Visibility="{Binding CanWrite, Converter={StaticResource BoolToVisibility}}" AutomationProperties.Name="Yeni gelir kaydı aç"/>
                <Button Style="{StaticResource Action}" Content="Yenile" Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </Grid>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,14,0,10">
            <Border Style="{StaticResource Panel}" Padding="14,10" Margin="0,0,7,0"><StackPanel><TextBlock Text="BUGÜN" Foreground="#65717E" FontSize="10"/><TextBlock Text="{Binding DailyTotal, StringFormat={}{0:C2}}" FontSize="22" FontWeight="SemiBold" Foreground="#156B63"/><TextBlock Text="{Binding Daily.TransactionCount, StringFormat={}{0:N0} işlem}" Foreground="#65717E"/></StackPanel></Border>
            <Border Style="{StaticResource Panel}" Padding="14,10" Margin="7,0"><StackPanel><TextBlock Text="BU HAFTA" Foreground="#65717E" FontSize="10"/><TextBlock Text="{Binding WeeklyTotal, StringFormat={}{0:C2}}" FontSize="22" FontWeight="SemiBold" Foreground="#17202A"/><TextBlock Text="{Binding Weekly.TransactionCount, StringFormat={}{0:N0} işlem}" Foreground="#65717E"/></StackPanel></Border>
            <Border Style="{StaticResource Panel}" Padding="14,10" Margin="7,0,0,0"><StackPanel><TextBlock Text="BU AY" Foreground="#65717E" FontSize="10"/><TextBlock Text="{Binding MonthlyTotal, StringFormat={}{0:C2}}" FontSize="22" FontWeight="SemiBold" Foreground="#17202A"/><TextBlock Text="{Binding Monthly.TransactionCount, StringFormat={}{0:N0} işlem}" Foreground="#65717E"/></StackPanel></Border>
        </UniformGrid>

        <TabControl Grid.Row="2" Background="White">
            <TabItem Header="İşlemler">
                <Grid Margin="10"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <WrapPanel Margin="0,0,0,7">
                        <DatePicker Style="{StaticResource Field}" Width="125" SelectedDate="{Binding FilterFrom}" AutomationProperties.Name="İşlem başlangıç tarihi"/>
                        <DatePicker Style="{StaticResource Field}" Width="125" SelectedDate="{Binding FilterTo}" AutomationProperties.Name="İşlem bitiş tarihi"/>
                        <ComboBox Style="{StaticResource Field}" Width="145" ItemsSource="{Binding IncomeTypes}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedFilterType}" ToolTip="Gelir türü"/>
                        <TextBox Style="{StaticResource Field}" Width="105" Text="{Binding FilterStudentNumber, UpdateSourceTrigger=PropertyChanged}" ToolTip="Tam öğrenci numarası" AutomationProperties.Name="Öğrenci filtresi"/>
                        <Button Style="{StaticResource Action}" Content="Öğrenciyi Bul" Command="{Binding LookupFilterStudentCommand}"/>
                        <TextBox Style="{StaticResource Field}" Width="115" Text="{Binding FilterCardNumber, UpdateSourceTrigger=PropertyChanged}" ToolTip="Tam kart numarası" AutomationProperties.Name="Kart filtresi"/>
                        <ComboBox Style="{StaticResource Field}" Width="95" ItemsSource="{Binding VoidStatuses}" DisplayMemberPath="Name" SelectedValuePath="Value" SelectedValue="{Binding FilterIsVoided}" ToolTip="İptal durumu"/>
                        <Button Style="{StaticResource Action}" Content="Filtrele" Command="{Binding ApplyFiltersCommand}" IsDefault="True"/>
                    </WrapPanel>
                    <DataGrid Grid.Row="1" x:Name="TransactionsGrid" ItemsSource="{Binding Transactions}" SelectedItem="{Binding SelectedTransaction}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column" RowHeaderWidth="0" RowHeight="30" ColumnHeaderHeight="32" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EDF0F2" AlternatingRowBackground="#FAFBFC" EnableRowVirtualization="True" EnableColumnVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True" AutomationProperties.Name="Kasa işlem listesi">
                        <DataGrid.InputBindings><KeyBinding Key="Delete" Command="{Binding OpenVoidCommand}"/><KeyBinding Key="Enter" Command="{Binding OpenVoidCommand}"/></DataGrid.InputBindings>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="TARİH" Binding="{Binding TransactionAt, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="125"/>
                            <DataGridTextColumn Header="ÖĞRENCİ" Binding="{Binding StudentName}" Width="*" MinWidth="145"/>
                            <DataGridTextColumn Header="KART" Binding="{Binding CardNumber}" Width="105"/>
                            <DataGridTextColumn Header="TÜR" Binding="{Binding IncomeTypeName}" Width="130"/>
                            <DataGridTextColumn Header="TUTAR" Binding="{Binding Amount, StringFormat={}{0:C2}}" Width="100"/>
                            <DataGridTextColumn Header="AÇIKLAMA" Binding="{Binding Description}" Width="*" MinWidth="130"/>
                            <DataGridCheckBoxColumn Header="İPTAL" Binding="{Binding IsVoided}" Width="55"/>
                            <DataGridTextColumn Header="İPTAL NEDENİ" Binding="{Binding VoidReason}" Width="140"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Grid Grid.Row="2" Margin="0,8,0,0"><TextBlock Text="{Binding FilterStudentText}" Foreground="#65717E" VerticalAlignment="Center"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Center"><Button Style="{StaticResource Action}" Content="Seçili İşlemi İptal Et" Command="{Binding OpenVoidCommand}" Visibility="{Binding CanWrite, Converter={StaticResource BoolToVisibility}}"/><TextBlock Text="Düzenleme ve silme desteklenmez." Foreground="#65717E" VerticalAlignment="Center"/></StackPanel><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><TextBlock Text="{Binding PageText}" Foreground="#65717E" VerticalAlignment="Center" Margin="0,0,12,0"/><Button Style="{StaticResource Action}" Content="Önceki" Command="{Binding PreviousPageCommand}"/><Button Style="{StaticResource Action}" Content="Sonraki" Command="{Binding NextPageCommand}"/></StackPanel></Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Günlük Kasa">
                <Grid Margin="14"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,16,0"><TextBlock Text="Günlük kasa" FontSize="16" FontWeight="SemiBold"/><WrapPanel Margin="0,10"><DatePicker Width="140" SelectedDate="{Binding DailyDate}"/><Button Style="{StaticResource Action}" Content="Göster" Command="{Binding LoadDailyCommand}" Margin="8,0,0,0"/></WrapPanel><TextBlock Text="{Binding Daily.TotalAmount, StringFormat=Net toplam: {0:C2}}" FontSize="20"/><TextBlock Text="{Binding Daily.VoidedAmount, StringFormat=İptal toplamı: {0:C2}}" Foreground="#9C342F"/><ItemsControl ItemsSource="{Binding Daily.ByIncomeType}" Margin="0,12,0,0"><ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding IncomeTypeName}"/><TextBlock Grid.Column="1" Text="{Binding Amount, StringFormat={}{0:C2}}" FontWeight="SemiBold"/></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel>
                    <StackPanel Grid.Column="1"><TextBlock Text="Özel tarih aralığı" FontSize="16" FontWeight="SemiBold"/><WrapPanel Margin="0,10"><DatePicker Width="130" SelectedDate="{Binding CustomFrom}"/><DatePicker Width="130" SelectedDate="{Binding CustomTo}" Margin="7,0"/><Button Style="{StaticResource Action}" Content="Hesapla" Command="{Binding LoadCustomCommand}"/></WrapPanel><TextBlock Text="{Binding Custom.TotalAmount, StringFormat=Net toplam: {0:C2}}" FontSize="20"/><TextBlock Text="{Binding Custom.TransactionCount, StringFormat={}{0:N0} aktif işlem}" Foreground="#65717E"/><ItemsControl ItemsSource="{Binding Custom.ByIncomeType}" Margin="0,12,0,0"><ItemsControl.ItemTemplate><DataTemplate><Grid Margin="0,3"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding IncomeTypeName}"/><TextBlock Grid.Column="1" Text="{Binding Amount, StringFormat={}{0:C2}}" FontWeight="SemiBold"/></Grid></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Gelir Türleri" Visibility="{Binding CanManage, Converter={StaticResource BoolToVisibility}}">
                <Grid Margin="14"><Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <DataGrid ItemsSource="{Binding ManagedTypes}" SelectedItem="{Binding SelectedManagedType}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"><DataGrid.Columns><DataGridTextColumn Header="AD" Binding="{Binding Name}" Width="*"/><DataGridCheckBoxColumn Header="AKTİF" Binding="{Binding IsActive}" Width="70"/></DataGrid.Columns></DataGrid>
                    <StackPanel Grid.Column="1" Margin="18,0,0,0"><TextBlock Text="Gelir türü" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="Ad" Style="{StaticResource Label}"/><TextBox Text="{Binding TypeName, UpdateSourceTrigger=PropertyChanged}"/><CheckBox Content="Aktif" IsChecked="{Binding TypeIsActive}" Margin="0,10"/><WrapPanel><Button Style="{StaticResource Action}" Content="Yeni" Command="{Binding NewTypeCommand}"/><Button Style="{StaticResource Action}" Content="Seçileni Düzenle" Command="{Binding EditTypeCommand}"/><Button Style="{StaticResource Action}" Content="Kaydet" Command="{Binding SaveTypeCommand}"/><Button Style="{StaticResource Action}" Content="Pasifleştir" Command="{Binding DeactivateTypeCommand}"/></WrapPanel></StackPanel>
                </Grid>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="3" Margin="0,8,0,0"><TextBlock Text="{Binding ErrorMessage}" Foreground="#9C342F"/><TextBlock Text="Filtrelerle eşleşen kasa işlemi bulunamadı." HorizontalAlignment="Center" Foreground="#65717E" Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}"/></Grid>
        <Border Grid.RowSpan="4" Background="#F7FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"><StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"><ProgressBar IsIndeterminate="True" Width="220" Height="3"/><TextBlock Text="Kasa verileri yükleniyor..." Margin="0,12,0,0" Foreground="#65717E"/></StackPanel></Border>

        <Border Grid.RowSpan="4" Background="White" BorderBrush="#CBD2D8" BorderThickness="1,0,0,0" Width="430" HorizontalAlignment="Right" Padding="20" Visibility="{Binding IsAddOpen, Converter={StaticResource BoolToVisibility}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel><Grid><TextBlock Text="Gelir Ekle" FontSize="20" FontWeight="SemiBold"/><Button Style="{StaticResource Action}" Content="Kapat" Command="{Binding CloseAddCommand}" HorizontalAlignment="Right"/></Grid><TextBlock Text="Tam öğrenci no veya kart no" Style="{StaticResource Label}"/><WrapPanel><TextBox Width="135" Text="{Binding StudentNumber, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Tam öğrenci numarası"/><TextBox Width="135" Margin="7,0" Text="{Binding LookupCardNumber, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Tam kart numarası"/><Button Style="{StaticResource Action}" Content="Doğrula" Command="{Binding LookupStudentCommand}"/></WrapPanel><TextBlock Text="{Binding LookupStudentText}" Foreground="#156B63" Margin="0,8" TextWrapping="Wrap"/><TextBlock Text="Gelir türü" Style="{StaticResource Label}"/><ComboBox ItemsSource="{Binding IncomeTypes}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedAddType}"/><TextBlock Text="Tarih ve saat" Style="{StaticResource Label}"/><WrapPanel><DatePicker Width="180" SelectedDate="{Binding AddDate}"/><TextBox Width="80" Margin="8,0" Text="{Binding TransactionTime, UpdateSourceTrigger=PropertyChanged}" ToolTip="SS:dd" AutomationProperties.Name="İşlem saati"/></WrapPanel><TextBlock Text="Tutar" Style="{StaticResource Label}"/><TextBox Text="{Binding AmountText, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Türk lirası tutarı" ToolTip="Örnek: 125,50 ₺"/><TextBlock Text="Açıklama" Style="{StaticResource Label}"/><TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" MaxLength="500" AcceptsReturn="True" Height="62"/><CheckBox IsChecked="{Binding AddConfirmed}" Margin="0,14,0,0" Content="Öğrenci, tür, tarih ve tutarı kontrol ederek kaydı onaylıyorum." AutomationProperties.Name="Gelir kaydını onayla"/><TextBlock Text="{Binding AddError}" Foreground="#9C342F" TextWrapping="Wrap" Margin="0,8"/><Button Style="{StaticResource Action}" Content="Onayla ve Kaydet" Command="{Binding AddCommand}" HorizontalAlignment="Left" Margin="0,8,0,0"/></StackPanel></ScrollViewer>
        </Border>

        <Border Grid.RowSpan="4" Background="White" BorderBrush="#CBD2D8" BorderThickness="1,0,0,0" Width="430" HorizontalAlignment="Right" Padding="20" Visibility="{Binding IsVoidOpen, Converter={StaticResource BoolToVisibility}}">
            <StackPanel><Grid><TextBlock Text="İşlemi İptal Et" FontSize="20" FontWeight="SemiBold" Foreground="#9C342F"/><Button Style="{StaticResource Action}" Content="Kapat" Command="{Binding CloseVoidCommand}" HorizontalAlignment="Right"/></Grid><TextBlock Text="Bu işlem düzenlenmez veya silinmez; denetim izi korunarak iptal edilir." TextWrapping="Wrap" Foreground="#65717E" Margin="0,14"/><Border Background="#FFF3E5" Padding="12"><TextBlock Text="{Binding VoidConfirmationText}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap"/></Border><TextBlock Text="İptal nedeni (zorunlu)" Style="{StaticResource Label}"/><TextBox Text="{Binding VoidReason, UpdateSourceTrigger=PropertyChanged}" MaxLength="500" AcceptsReturn="True" Height="75"/><CheckBox IsChecked="{Binding VoidConfirmed}" Margin="0,14" Content="Yukarıdaki kesin tutar ve öğrenciye ait işlemi iptal ettiğimi onaylıyorum." AutomationProperties.Name="İşlem iptalini onayla"/><Button Style="{StaticResource Action}" Content="Onayla ve İptal Et" Command="{Binding VoidCommand}" HorizontalAlignment="Left"/></StackPanel>
        </Border>
    </Grid>
</UserControl>
