<UserControl x:Class="Yemekhane.Desktop.Views.SmsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Yemekhane.Desktop.Controls"
    FontFamily="Segoe UI" FontSize="12">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Yemekhane.Desktop;component/Themes/DesignSystem.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        <BooleanToVisibilityConverter x:Key="Bool"/>
            </ResourceDictionary>
</UserControl.Resources>
    <controls:PageShell Title="SMS Merkezi" Subtitle="Veli bildirimlerini önizleyin, kuyruğa alın ve teslimatı izleyin">
        <controls:PageShell.Actions>
            <Border Background="{StaticResource WarningSoftBrush}" CornerRadius="12" Padding="9,4" Visibility="{Binding IsOffline, Converter={StaticResource Bool}}"><TextBlock Text="Çevrimdışı" Foreground="{StaticResource WarningBrush}"/></Border>
        </controls:PageShell.Actions>

        <TabControl>
            <TabItem Header="Gönder" Visibility="{Binding CanSend, Converter={StaticResource Bool}}">
                <Grid Margin="14"><Grid.ColumnDefinitions><ColumnDefinition Width="1.05*"/><ColumnDefinition Width="0.95*"/></Grid.ColumnDefinitions>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="0,0,14,0">
                        <TextBlock Text="1. Alıcı kapsamı" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <WrapPanel><ComboBox Style="{StaticResource FieldCompactCombo}" Width="130" ItemsSource="{Binding TargetTypes}" SelectedItem="{Binding TargetType}" AutomationProperties.Name="SMS hedef türü"/><TextBox Style="{StaticResource FieldCompact}" Width="220" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" ToolTip="Öğrenci arama veya filtre"/><Button Content="Ara" Command="{Binding SearchStudentsCommand}"/></WrapPanel>
                        <WrapPanel><ComboBox Style="{StaticResource FieldCompactCombo}" Width="220" ItemsSource="{Binding Classes}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedClass}" ToolTip="Sınıf hedefi"/><ComboBox Style="{StaticResource FieldCompactCombo}" Width="220" ItemsSource="{Binding Groups}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedGroup}" ToolTip="Grup hedefi"/></WrapPanel>
                        <Grid Height="190"><DataGrid ItemsSource="{Binding Students}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="False" AutomationProperties.Name="Manuel SMS öğrenci seçimi"><DataGrid.Columns><DataGridCheckBoxColumn Header="Seç" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="52"/><DataGridTextColumn Header="No" Binding="{Binding StudentNo}" IsReadOnly="True" Width="90"/><DataGridTextColumn Header="Öğrenci" Binding="{Binding Name}" IsReadOnly="True" Width="*"/></DataGrid.Columns></DataGrid><TextBlock Text="Eşleşen aktif öğrenci bulunamadı." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" Visibility="{Binding IsSendTargetsEmpty, Converter={StaticResource Bool}}"/></Grid>
                        <TextBlock Text="2. Mesaj" FontSize="15" FontWeight="SemiBold" Margin="0,16,0,8"/>
                        <CheckBox Content="Şablon kullan" IsChecked="{Binding UseTemplate}" Margin="0,0,0,8"/><ComboBox Style="{StaticResource FieldCompactCombo}" ItemsSource="{Binding Templates}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedTemplate}" AutomationProperties.Name="SMS şablonu"/>
                        <WrapPanel><TextBox Style="{StaticResource FieldCompact}" Width="130" Text="{Binding ExpiryDate, UpdateSourceTrigger=PropertyChanged}" ToolTip="Son tarih değişkeni"/><TextBox Style="{StaticResource FieldCompact}" Width="110" Text="{Binding EntryTime, UpdateSourceTrigger=PropertyChanged}" ToolTip="Giriş saati değişkeni"/><TextBox Style="{StaticResource FieldCompact}" Width="110" Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" ToolTip="Tutar değişkeni"/></WrapPanel>
                        <TextBox Text="{Binding CustomMessage, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="95" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" AutomationProperties.Name="Özel SMS mesajı"/>
                        <TextBlock Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Right"><Run Text="{Binding CharacterCount, Mode=OneWay}"/><Run Text=" karakter • "/><Run Text="{Binding SegmentCount, Mode=OneWay}"/><Run Text=" SMS segmenti"/></TextBlock>
                        <WrapPanel Margin="0,12,0,0"><Button Content="Alıcıları ve mesajı önizle" Command="{Binding PreviewCommand}"/><ProgressBar IsIndeterminate="True" Width="120" Height="3" Visibility="{Binding IsSendLoading, Converter={StaticResource Bool}}"/></WrapPanel>
                        <TextBlock Text="{Binding SendError}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,8,0,0"/>
                    </StackPanel></ScrollViewer>
                    <Border Grid.Column="1" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="7" Padding="16">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel>
                            <TextBlock Text="Gönderim önizlemesi" FontSize="15" FontWeight="SemiBold"/>
                            <TextBlock Text="Önizleme oluşturmadan hiçbir SMS kuyruğa alınmaz." Foreground="{StaticResource MutedBrush}" Margin="0,5,0,12"/>
                            <Grid Visibility="{Binding HasPreview, Converter={StaticResource Bool}}"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                <UniformGrid Columns="4"><StackPanel><TextBlock Text="EŞLEŞEN" Foreground="{StaticResource MutedBrush}"/><TextBlock Text="{Binding Preview.MatchedStudents}" FontSize="22"/></StackPanel><StackPanel><TextBlock Text="ALICI" Foreground="{StaticResource MutedBrush}"/><TextBlock Text="{Binding Preview.RecipientCount}" FontSize="22" Foreground="{StaticResource AccentBrush}"/></StackPanel><StackPanel><TextBlock Text="TELEFON YOK" Foreground="{StaticResource MutedBrush}"/><TextBlock Text="{Binding Preview.NoPhoneCount}" FontSize="22" Foreground="{StaticResource DangerBrush}"/></StackPanel><StackPanel><TextBlock Text="MÜKERRER" Foreground="{StaticResource MutedBrush}"/><TextBlock Text="{Binding Preview.DuplicatePhoneCount}" FontSize="22" Foreground="{StaticResource WarningBrush}"/></StackPanel></UniformGrid>
                                <ItemsControl Grid.Row="1" ItemsSource="{Binding Preview.Examples}" Margin="0,14,0,10"><ItemsControl.ItemTemplate><DataTemplate><Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="0,8"><StackPanel><TextBlock Text="{Binding StudentName}" FontWeight="SemiBold"/><TextBlock Text="{Binding ParentName, StringFormat=Veli: {0}}" Foreground="{StaticResource MutedBrush}"/><TextBlock Text="{Binding Phone}" Foreground="{StaticResource MutedBrush}"/><TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0,4,0,0"/></StackPanel></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
                                <StackPanel Grid.Row="2"><CheckBox IsChecked="{Binding IsConfirmed}" Margin="0,5,0,10"><TextBlock Text="Alıcı sayısını ve örnek mesajları kontrol ettim; kuyruğa alınmasını onaylıyorum." TextWrapping="Wrap"/></CheckBox><Button Content="SMS'leri kuyruğa al" Command="{Binding EnqueueCommand}" HorizontalAlignment="Left" Background="{StaticResource AccentBrush}" Foreground="White"/></StackPanel>
                            </Grid>
                            <TextBlock Text="{Binding EnqueueResultText}" Foreground="{StaticResource AccentBrush}" TextWrapping="Wrap" Margin="0,10,0,0"/>
                        </StackPanel></ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>
            <TabItem Header="Şablonlar" Visibility="{Binding CanManage, Converter={StaticResource Bool}}">
                <Grid Margin="14"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="340"/></Grid.ColumnDefinitions>
                    <Grid Margin="0,0,14,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><WrapPanel><CheckBox Content="Pasifleri göster" IsChecked="{Binding IncludeInactive}" Margin="0,0,12,0" VerticalAlignment="Center"/><Button Content="Yenile" Command="{Binding RefreshTemplatesCommand}"/><Button Content="Yeni şablon" Command="{Binding NewTemplateCommand}"/></WrapPanel><Grid Grid.Row="1"><DataGrid ItemsSource="{Binding Templates}" SelectedItem="{Binding SelectedTemplate}" AutoGenerateColumns="False"><DataGrid.Columns><DataGridTextColumn Header="Ad" Binding="{Binding Name}" Width="180"/><DataGridTextColumn Header="Metin" Binding="{Binding Body}" Width="*"/><DataGridCheckBoxColumn Header="Aktif" Binding="{Binding IsActive}" Width="60"/></DataGrid.Columns><DataGrid.InputBindings><MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditTemplateCommand}"/></DataGrid.InputBindings></DataGrid><TextBlock Text="Henüz SMS şablonu yok." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" Visibility="{Binding IsTemplatesEmpty, Converter={StaticResource Bool}}"/><ProgressBar IsIndeterminate="True" Width="160" Height="3" VerticalAlignment="Center" Visibility="{Binding IsTemplatesLoading, Converter={StaticResource Bool}}"/></Grid></Grid>
                    <Border Grid.Column="1" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="7" Padding="16"><StackPanel><TextBlock Text="Şablon düzenleyici" FontSize="15" FontWeight="SemiBold"/><TextBlock Text="Ad" Margin="0,12,0,4"/><TextBox Text="{Binding TemplateName, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Text="Metin" Margin="0,10,0,4"/><TextBox Text="{Binding TemplateBody, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Height="150"/><TextBlock Text="Değişkenler: {{StudentName}}, {{ParentName}}, {{ExpiryDate}}, {{EntryTime}}, {{Amount}}" TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,8"/><WrapPanel><Button Content="Kaydet" Command="{Binding SaveTemplateCommand}"/><Button Content="Pasifleştir" Command="{Binding DeactivateTemplateCommand}"/></WrapPanel><TextBlock Text="{Binding TemplateError}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,8,0,0"/></StackPanel></Border>
                </Grid>
            </TabItem>
            <TabItem Header="Geçmiş" Visibility="{Binding CanRead, Converter={StaticResource Bool}}">
                <Grid Margin="14"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <WrapPanel><DatePicker Style="{StaticResource FieldCompactDate}" Width="130" SelectedDate="{Binding HistoryFrom}"/><DatePicker Style="{StaticResource FieldCompactDate}" Width="130" SelectedDate="{Binding HistoryTo}"/><TextBox Style="{StaticResource FieldCompact}" Width="140" Text="{Binding HistoryStudent}" ToolTip="Öğrenci adı veya no"/><TextBox Style="{StaticResource FieldCompact}" Width="130" Text="{Binding HistoryPhone}" ToolTip="Telefon"/><TextBox Style="{StaticResource FieldCompact}" Width="120" Text="{Binding HistoryProvider}" ToolTip="Provider"/><ComboBox Style="{StaticResource FieldCompactCombo}" Width="130" ItemsSource="{Binding HistoryStatuses}" SelectedItem="{Binding HistoryStatus}"/><Button Content="Filtrele / Yenile" Command="{Binding RefreshHistoryCommand}"/></WrapPanel>
                    <Grid Grid.Row="1"><DataGrid ItemsSource="{Binding History}" AutoGenerateColumns="False"><DataGrid.Columns><DataGridTextColumn Header="Tarih" Binding="{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="130"/><DataGridTextColumn Header="Telefon" Binding="{Binding Phone}" Width="120"/><DataGridTextColumn Header="Provider" Binding="{Binding Provider}" Width="95"/><DataGridTemplateColumn Header="Durum" Width="110"><DataGridTemplateColumn.CellTemplate><DataTemplate><Border CornerRadius="9" Padding="7,3"><Border.Style><Style TargetType="Border"><Setter Property="Background" Value="{StaticResource SunkenBrush}"/><Style.Triggers><DataTrigger Binding="{Binding Status}" Value="Sent"><Setter Property="Background" Value="{StaticResource SuccessSoftBrush}"/></DataTrigger><DataTrigger Binding="{Binding Status}" Value="Failed"><Setter Property="Background" Value="{StaticResource DangerSoftBrush}"/></DataTrigger><DataTrigger Binding="{Binding Status}" Value="RetryScheduled"><Setter Property="Background" Value="{StaticResource WarningSoftBrush}"/></DataTrigger><DataTrigger Binding="{Binding Status}" Value="Sending"><Setter Property="Background" Value="{StaticResource InfoSoftBrush}"/></DataTrigger></Style.Triggers></Style></Border.Style><TextBlock Text="{Binding Status}" HorizontalAlignment="Center"/></Border></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn><DataGridTextColumn Header="Mesaj" Binding="{Binding Message}" Width="*"/><DataGridTextColumn Header="Hata" Binding="{Binding Error}" Width="180"/><DataGridTextColumn Header="Deneme" Binding="{Binding AttemptCount}" Width="60"/><DataGridTemplateColumn Header="İşlem" Width="90"><DataGridTemplateColumn.CellTemplate><DataTemplate><Button Content="Tekrar dene" Command="{Binding DataContext.RetryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn></DataGrid.Columns></DataGrid><TextBlock Text="SMS geçmişi boş." HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsHistoryEmpty, Converter={StaticResource Bool}}"/><ProgressBar IsIndeterminate="True" Width="180" Height="3" VerticalAlignment="Center" Visibility="{Binding IsHistoryLoading, Converter={StaticResource Bool}}"/></Grid>
                    <Grid Grid.Row="2" Margin="0,9,0,0"><TextBlock Text="{Binding HistoryError}" Foreground="{StaticResource DangerBrush}"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="Önceki" Command="{Binding PreviousHistoryPageCommand}"/><TextBlock Text="{Binding HistoryPage, StringFormat=Sayfa {0}}" VerticalAlignment="Center" Margin="4,0,12,0"/><Button Content="Sonraki" Command="{Binding NextHistoryPageCommand}"/></StackPanel></Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </controls:PageShell>
</UserControl>
