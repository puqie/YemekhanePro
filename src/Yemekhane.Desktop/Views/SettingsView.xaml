<UserControl x:Class="Yemekhane.Desktop.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Yemekhane.Desktop"
    xmlns:controls="clr-namespace:Yemekhane.Desktop.Controls"
    FontFamily="Segoe UI" FontSize="12">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Yemekhane.Desktop;component/Themes/DesignSystem.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        <BooleanToVisibilityConverter x:Key="Bool"/>
        <local:InverseBooleanConverter x:Key="InverseBool"/>
            </ResourceDictionary>
</UserControl.Resources>
    <controls:PageShell Title="Sistem Ayarları" Subtitle="Okul, sağlayıcı, yedekleme, senkronizasyon ve günlük yapılandırması">
        <controls:PageShell.Actions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="{StaticResource WarningSoftBrush}" CornerRadius="12" Padding="9,4" Margin="0,0,8,0" Visibility="{Binding IsOffline, Converter={StaticResource Bool}}"><TextBlock Text="Çevrimdışı" Foreground="{StaticResource WarningBrush}"/></Border>
                <Button Content="Yenile" Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </controls:PageShell.Actions>

        <controls:PageShell.FooterLeft>
            <StackPanel><TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"/><TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource AccentBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/></StackPanel>
        </controls:PageShell.FooterLeft>
        <controls:PageShell.FooterRight>
            <StackPanel Orientation="Horizontal" Visibility="{Binding CanManage, Converter={StaticResource Bool}}"><TextBlock Text="Kaydedilmemiş değişiklikler" Foreground="{StaticResource WarningBrush}" VerticalAlignment="Center" Margin="0,0,12,0" Visibility="{Binding IsDirty, Converter={StaticResource Bool}}"/><Button Content="İptal" Command="{Binding CancelCommand}"/><Button Content="Kaydet" Command="{Binding SaveCommand}" Background="{StaticResource AccentBrush}" Foreground="White"/></StackPanel>
        </controls:PageShell.FooterRight>

        <Grid>
            <TabControl IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBool}}">
                <TabItem Header="Okul">
                    <ScrollViewer><StackPanel Margin="18" Width="760" HorizontalAlignment="Left">
                        <TextBlock Text="Okul bilgileri" FontSize="16" FontWeight="SemiBold"/><TextBlock Style="{StaticResource Label}" Text="Okul adı"/><TextBox Style="{StaticResource Field}" Text="{Binding SchoolName, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding CanManage, Converter={StaticResource InverseBool}}"/>
                        <TextBlock Style="{StaticResource Label}" Text="Adres"/><TextBox Style="{StaticResource Field}" Height="70" Margin="0,3,0,9" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding SchoolAddress, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding CanManage, Converter={StaticResource InverseBool}}"/>
                        <TextBlock Style="{StaticResource Label}" Text="İletişim"/><TextBox Style="{StaticResource Field}" Text="{Binding SchoolContact, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding CanManage, Converter={StaticResource InverseBool}}"/>
                        <TextBlock Style="{StaticResource Label}" Text="Logo dosya yolu"/><TextBox Style="{StaticResource Field}" Text="{Binding LogoPath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding CanManage, Converter={StaticResource InverseBool}}"/>
                    </StackPanel></ScrollViewer>
                </TabItem>
                <TabItem Header="Bağlantılar">
                    <StackPanel Margin="18"><TextBlock Text="Yönetim ekranları" FontSize="16" FontWeight="SemiBold"/><WrapPanel Margin="0,14,0,0"><Button Command="{Binding NavigateDevicesCommand}" Content="Cihazlar / Kart Okuyucular"/><TextBlock Text="{Binding DeviceCount, StringFormat={}  {0} kayıt}" VerticalAlignment="Center"/></WrapPanel><ItemsControl ItemsSource="{Binding DeviceSummaries}" Margin="12,0,0,8"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" Foreground="{StaticResource MutedBrush}"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><WrapPanel Margin="0,10,0,0"><Button Command="{Binding NavigateMealsCommand}" Content="Yemek Türleri"/><TextBlock Text="{Binding MealTypeCount, StringFormat={}  {0} aktif tür}" VerticalAlignment="Center"/></WrapPanel><ItemsControl ItemsSource="{Binding MealTypes}" Margin="12,0,0,8"><ItemsControl.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" Foreground="{StaticResource MutedBrush}"/></DataTemplate></ItemsControl.ItemTemplate></ItemsControl><Button Command="{Binding NavigateHolidaysCommand}" Content="Tatiller / Takvim" HorizontalAlignment="Left" Margin="0,10,0,0"/><Button Command="{Binding NavigateUsersCommand}" Content="Kullanıcılar / Roller" HorizontalAlignment="Left" Margin="0,10,0,0" Visibility="{Binding CanNavigateUsers, Converter={StaticResource Bool}}"/><TextBlock Text="Cihaz yapılandırması ilgili yönetim ekranında yapılır." Foreground="{StaticResource MutedBrush}" Margin="0,16,0,0"/></StackPanel>
                </TabItem>
                <TabItem Header="SMS">
                    <StackPanel Margin="18" Width="760" HorizontalAlignment="Left"><TextBlock Text="SMS sağlayıcısı" FontSize="16" FontWeight="SemiBold"/><TextBlock Style="{StaticResource Label}" Text="Endpoint"/><TextBox Style="{StaticResource Field}" Text="{Binding SmsEndpoint, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Style="{StaticResource Label}" Text="Kimlik doğrulama"/><ComboBox Style="{StaticResource FieldCombo}" ItemsSource="{Binding SmsAuthTypes}" SelectedItem="{Binding SmsAuthType}"/><TextBlock Style="{StaticResource Label}" Text="Kullanıcı adı"/><TextBox Style="{StaticResource Field}" Text="{Binding SmsUsername, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Style="{StaticResource Label}" Text="Gönderici"/><TextBox Style="{StaticResource Field}" Text="{Binding SmsSender, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Style="{StaticResource Label}" Text="Zaman aşımı (saniye)"/><TextBox Style="{StaticResource Field}" Text="{Binding SmsTimeoutSeconds}"/><TextBlock Style="{StaticResource Label}" Text="Gizli bilgi (boş bırakırsanız değişmez)"/><PasswordBox x:Name="SmsSecretBox" Height="30" Margin="0,3,0,3" PasswordChanged="SmsSecretChanged"/><TextBlock Text="Gizli bilgi yapılandırıldı" Foreground="{StaticResource AccentBrush}" Visibility="{Binding SmsSecretConfigured, Converter={StaticResource Bool}}"/><TextBlock Text="Sağlayıcı değişiklikleri API yeniden başlatıldığında uygulanır." Foreground="{StaticResource WarningBrush}" Margin="0,12,0,0"/></StackPanel>
                </TabItem>
                <TabItem Header="Yedekleme">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="18" Width="760" HorizontalAlignment="Left">
                        <TextBlock Text="Zamanlama" FontSize="16" FontWeight="SemiBold"/>
                        <TextBlock Text="Otomatik yedekleme burada ayarlanır; alttaki elle işlemler her zaman kullanılabilir." Foreground="{StaticResource MutedBrush}" Margin="0,3,0,10" TextWrapping="Wrap"/>
                        <CheckBox Content="Otomatik yedeklemeyi etkinleştir" IsChecked="{Binding BackupEnabled}"/>
                        <WrapPanel Margin="0,10,0,0">
                            <StackPanel Width="180"><TextBlock Style="{StaticResource Label}" Text="Sıklık"/><ComboBox Style="{StaticResource FieldCombo}" ItemsSource="{Binding BackupFrequencies}" SelectedItem="{Binding BackupFrequency}"/></StackPanel>
                            <StackPanel Width="180" Margin="10,0"><TextBlock Style="{StaticResource Label}" Text="Haftanın günü"/><ComboBox Style="{StaticResource FieldCombo}" ItemsSource="{Binding WeekDays}" SelectedItem="{Binding BackupWeeklyDay}"/></StackPanel>
                            <StackPanel Width="120"><TextBlock Style="{StaticResource Label}" Text="Saat (HH:mm)"/><TextBox Style="{StaticResource Field}" Text="{Binding BackupTime}"/></StackPanel>
                        </WrapPanel>
                        <TextBlock Style="{StaticResource Label}" Text="Saklanacak yedek sayısı"/>
                        <TextBox Style="{StaticResource Field}" Width="140" HorizontalAlignment="Left" Text="{Binding BackupRetentionCount}"/>
                        <TextBlock Text="Bu sayıyı aşan en eski DÜZENLİ yedekler otomatik silinir. Geri yükleme öncesi alınan güvenlik yedekleri AYRI bir kotada tutulur (son 3), bu sayacı tüketmez." Foreground="{StaticResource FaintBrush}" TextWrapping="Wrap" Margin="0,4,0,10"/>
                        <TextBlock Style="{StaticResource Label}" Text="Yedek klasörü"/>
                        <TextBox Style="{StaticResource Field}" Text="{Binding BackupPath}"/>

                        <Separator Margin="0,18,0,14"/>

                        <TextBlock Text="Elle işlemler" FontSize="16" FontWeight="SemiBold"/>
                        <TextBlock Text="Anlık yedek alın, bir yedek dosyasını doğrulayın veya geri yükleyin." Foreground="{StaticResource MutedBrush}" Margin="0,3,0,10" TextWrapping="Wrap"/>
                        <WrapPanel Margin="0,0,0,10"><Button Content="Şimdi Yedekle" Command="{Binding BackupNowCommand}"/><Button Content="Yedek Dosyası Seç" Command="{Binding ChooseRestoreCommand}"/><Button Content="Yedeği Doğrula" Command="{Binding ValidateBackupCommand}"/></WrapPanel>
                        <TextBlock Text="{Binding RestorePath}" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap"/>
                        <TextBlock Style="{StaticResource Label}" Text="Geri yükleme onayı: GERI YUKLE"/>
                        <TextBox Style="{StaticResource Field}" Text="{Binding RestoreConfirmation, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Geri Yükle" Command="{Binding RestoreCommand}" Style="{StaticResource Destructive}" HorizontalAlignment="Left" Margin="0,4,0,0"/>
                        <TextBlock Text="Geri yükleme önce güvenlik yedeği alır ve uygulamanın yeniden başlatılmasını gerektirir." Foreground="{StaticResource WarningBrush}" Margin="0,10,0,0" TextWrapping="Wrap"/>
                    </StackPanel></ScrollViewer>
                </TabItem>
                <TabItem Header="Sync">
                    <StackPanel Margin="18" Width="760" HorizontalAlignment="Left"><CheckBox Content="Senkronizasyonu etkinleştir" IsChecked="{Binding SyncEnabled}"/><TextBlock Style="{StaticResource Label}" Text="Endpoint"/><TextBox Style="{StaticResource Field}" Text="{Binding SyncEndpoint, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Style="{StaticResource Label}" Text="Cihaz kimliği"/><TextBox Style="{StaticResource Field}" Text="{Binding SyncDeviceId, UpdateSourceTrigger=PropertyChanged}"/><TextBlock Style="{StaticResource Label}" Text="Çalışma aralığı (dakika)"/><TextBox Style="{StaticResource Field}" Text="{Binding SyncIntervalMinutes}"/><TextBlock Style="{StaticResource Label}" Text="Erişim gizlisi (boş bırakırsanız değişmez)"/><PasswordBox x:Name="SyncSecretBox" Height="30" Margin="0,3,0,3" PasswordChanged="SyncSecretChanged"/><TextBlock Text="Gizli bilgi yapılandırıldı" Foreground="{StaticResource AccentBrush}" Visibility="{Binding SyncSecretConfigured, Converter={StaticResource Bool}}"/><TextBlock Text="{Binding SyncStatusText}" Margin="0,14,0,8"/><StackPanel Orientation="Horizontal"><Button Content="Şimdi Senkronize Et" Command="{Binding SyncNowCommand}"/><Button Content="Çakışmaları Yenile" Command="{Binding RefreshConflictsCommand}" Margin="7,0,0,0"/></StackPanel><TextBlock Text="Zamanlama değişiklikleri servis yeniden başlatıldığında uygulanır; elle çalıştırma kaydedilmiş endpoint'i hemen kullanır." Foreground="{StaticResource WarningBrush}" TextWrapping="Wrap" Margin="0,10,0,0"/><StackPanel Margin="0,16,0,0" Visibility="{Binding HasConflicts, Converter={StaticResource Bool}}"><TextBlock Text="Çözüm bekleyen çakışmalar" FontWeight="SemiBold"/><TextBlock Text="Sunucudaki kayıt bu işlemden sonra değişmiş. Yeniden kuyruğa alırsanız işlem tekrar gönderilir." Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,3,0,7"/><DataGrid ItemsSource="{Binding Conflicts}" SelectedItem="{Binding SelectedConflict}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" MaxHeight="200" RowHeight="24" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"><DataGrid.Columns><DataGridTextColumn Header="Zaman" Binding="{Binding Timestamp, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="130"/><DataGridTextColumn Header="Kayıt" Binding="{Binding EntityName}" Width="110"/><DataGridTextColumn Header="İşlem" Binding="{Binding OperationType}" Width="150"/><DataGridTextColumn Header="Deneme" Binding="{Binding AttemptCount}" Width="70"/><DataGridTextColumn Header="Neden" Binding="{Binding LastError}" Width="*"/></DataGrid.Columns></DataGrid><Button Content="Seçileni Yeniden Kuyruğa Al" Command="{Binding RequeueConflictCommand}" HorizontalAlignment="Left" Margin="0,8,0,0"/></StackPanel></StackPanel>
                </TabItem>
                <TabItem Header="Loglar">
                    <Grid Margin="18"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><WrapPanel><TextBlock Text="Seviye" VerticalAlignment="Center" Margin="0,0,8,0"/><ComboBox Width="150" ItemsSource="{Binding LogLevels}" SelectedItem="{Binding LogLevel}"/><TextBlock Text="Saklama günü" VerticalAlignment="Center" Margin="16,0,8,0"/><TextBox Width="70" Text="{Binding LogRetentionDays}"/><TextBlock Text="Dosya yolu" VerticalAlignment="Center" Margin="16,0,8,0"/><TextBox Width="220" Text="{Binding LogPath}"/><Button Content="Logları Yenile" Command="{Binding RefreshLogsCommand}" Margin="12,0,0,0"/></WrapPanel><DataGrid Grid.Row="1" ItemsSource="{Binding Logs}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,12,0,0"><DataGrid.Columns><DataGridTextColumn Header="Zaman" Binding="{Binding Timestamp, StringFormat={}{0:dd.MM.yyyy HH:mm:ss}}" Width="145"/><DataGridTextColumn Header="Seviye" Binding="{Binding Level}" Width="85"/><DataGridTextColumn Header="Kaynak" Binding="{Binding Source}" Width="160"/><DataGridTextColumn Header="Mesaj" Binding="{Binding Message}" Width="*"/><DataGridTextColumn Header="Özellikler" Binding="{Binding Properties}" Width="220"/></DataGrid.Columns></DataGrid></Grid>
                </TabItem>
            </TabControl>
            <ProgressBar IsIndeterminate="True" Width="220" Height="3" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource Bool}}"/>
        </Grid>
    </controls:PageShell>
</UserControl>
