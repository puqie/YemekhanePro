<UserControl x:Class="Yemekhane.Desktop.Views.MealEntitlementsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:views="clr-namespace:Yemekhane.Desktop.Views"
    Background="#F4F5F7" FontFamily="Segoe UI" FontSize="12">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <Style x:Key="Field" TargetType="Control"><Setter Property="Height" Value="30"/><Setter Property="Margin" Value="0,0,7,7"/></Style>
        <Style x:Key="Action" TargetType="Button"><Setter Property="Padding" Value="11,6"/><Setter Property="Margin" Value="0,0,7,0"/><Setter Property="Background" Value="White"/><Setter Property="BorderBrush" Value="#D7DDE2"/><Setter Property="Cursor" Value="Hand"/></Style>
        <Style x:Key="Label" TargetType="TextBlock"><Setter Property="Foreground" Value="#65717E"/><Setter Property="Margin" Value="0,7,0,3"/></Style>
    </UserControl.Resources>
    <Grid Margin="22,18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid>
            <StackPanel><TextBlock Text="Yemek Hakedişleri" FontSize="22" FontWeight="SemiBold" Foreground="#17202A"/><TextBlock Text="Öğrenci bazlı hak, kullanım ve kalan miktar yönetimi" Foreground="#65717E" Margin="0,4,0,0"/></StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Border Background="#FFF3E5" CornerRadius="12" Padding="9,4" Margin="0,0,10,0" Visibility="{Binding IsOffline, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="Çevrimdışı" Foreground="#8A681D"/></Border>
                <Button Style="{StaticResource Action}" Content="Seçileni İptal Et" Command="{Binding RequestCancelCommand}" Visibility="{Binding CanManage, Converter={StaticResource BoolToVisibility}}"/>
                <Button Style="{StaticResource Action}" Content="+ Hızlı Hakediş" Command="{Binding OpenGrantCommand}" Visibility="{Binding CanBulk, Converter={StaticResource BoolToVisibility}}"/>
                <Button Style="{StaticResource Action}" Content="Toplu İşlem" Command="{Binding OpenBulkCommand}" Visibility="{Binding CanBulk, Converter={StaticResource BoolToVisibility}}"/>
                <Button Style="{StaticResource Action}" Content="Yenile" Command="{Binding SearchCommand}"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="White" BorderBrush="#E1E5E9" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,14,0,10">
            <WrapPanel>
                <DatePicker Style="{StaticResource Field}" Width="125" SelectedDate="{Binding StartsOn}" ToolTip="Başlangıç tarihi"/>
                <DatePicker Style="{StaticResource Field}" Width="125" SelectedDate="{Binding EndsOn}" ToolTip="Bitiş tarihi"/>
                <TextBox Style="{StaticResource Field}" Width="95" Text="{Binding StudentNo}" ToolTip="Öğrenci NO"/>
                <TextBox Style="{StaticResource Field}" Width="105" Text="{Binding CardNumber}" ToolTip="Kart no"/>
                <TextBox Style="{StaticResource Field}" Width="150" Text="{Binding StudentName}" ToolTip="Ad soyad"/>
                <TextBox Style="{StaticResource Field}" Width="105" Text="{Binding ClassName}" ToolTip="Sınıf"/>
                <ComboBox Style="{StaticResource Field}" Width="120" ItemsSource="{Binding Groups}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedGroup}" ToolTip="Grup"/>
                <ComboBox Style="{StaticResource Field}" Width="110" ItemsSource="{Binding MealTypes}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedMeal}" ToolTip="Öğün"/>
                <ComboBox Style="{StaticResource Field}" Width="95" ItemsSource="{Binding Statuses}" DisplayMemberPath="Name" SelectedValuePath="Value" SelectedValue="{Binding Status}" ToolTip="Durum"/>
                <Button Style="{StaticResource Action}" Content="Filtrele" Command="{Binding SearchCommand}" IsDefault="True"/>
            </WrapPanel>
        </Border>

        <UniformGrid Grid.Row="2" Columns="3" Margin="0,0,0,10">
            <Border Background="White" BorderBrush="#E1E5E9" BorderThickness="1" Padding="12" Margin="0,0,5,0"><StackPanel><TextBlock Text="TOPLAM HAK" Foreground="#65717E" FontSize="10"/><TextBlock Text="{Binding TotalQuantity, StringFormat=N0}" FontSize="20" FontWeight="SemiBold"/></StackPanel></Border>
            <Border Background="White" BorderBrush="#E1E5E9" BorderThickness="1" Padding="12" Margin="5,0"><StackPanel><TextBlock Text="KULLANILAN" Foreground="#65717E" FontSize="10"/><TextBlock Text="{Binding ConsumedQuantity, StringFormat=N0}" FontSize="20" FontWeight="SemiBold" Foreground="#156B63"/></StackPanel></Border>
            <Border Background="White" BorderBrush="#E1E5E9" BorderThickness="1" Padding="12" Margin="5,0,0,0"><StackPanel><TextBlock Text="KALAN" Foreground="#65717E" FontSize="10"/><TextBlock Text="{Binding RemainingQuantity, StringFormat=N0}" FontSize="20" FontWeight="SemiBold" Foreground="#356890"/></StackPanel></Border>
        </UniformGrid>

        <Border Grid.Row="3" Background="White" BorderBrush="#E1E5E9" BorderThickness="1" CornerRadius="6">
            <Grid>
                <DataGrid x:Name="EntitlementsGrid" ItemsSource="{Binding Items}" SelectionMode="Extended" SelectionUnit="FullRow" SelectionChanged="OnSelectionChanged"
                    AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column" RowHeaderWidth="0"
                    RowHeight="29" ColumnHeaderHeight="32" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EDF0F2" AlternatingRowBackground="#FAFBFC"
                    EnableRowVirtualization="True" EnableColumnVirtualization="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True" AutomationProperties.Name="Yemek hakediş listesi">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="TARİH" Binding="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" Width="82"/>
                        <DataGridTextColumn Header="NO" Binding="{Binding StudentNo}" Width="72"/>
                        <DataGridTextColumn Header="KART NO" Binding="{Binding CardNumber}" Width="92"/>
                        <DataGridTextColumn Header="ÖĞÜN" Binding="{Binding MealName}" Width="82"/>
                        <DataGridTextColumn Header="AD SOYAD" Binding="{Binding StudentName}" Width="*" MinWidth="145"/>
                        <DataGridTextColumn Header="SINIF" Binding="{Binding ClassName}" Width="72"/>
                        <DataGridTextColumn Header="ADET" Binding="{Binding Quantity}" Width="52"/>
                        <DataGridTextColumn Header="KULL." Binding="{Binding ConsumedQuantity}" Width="52"/>
                        <DataGridTextColumn Header="KALAN" Binding="{Binding RemainingQuantity}" Width="55"/>
                        <DataGridTextColumn Header="DURUM" Binding="{Binding Status}" Width="76"/>
                        <DataGridTextColumn Header="KAYNAK" Binding="{Binding Source}" Width="115"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"><ProgressBar IsIndeterminate="True" Width="220" Height="3"/><TextBlock Text="Hakedişler yükleniyor..." Margin="0,12,0,0" Foreground="#65717E"/></StackPanel>
                <TextBlock Text="Filtrelerle eşleşen hakediş bulunamadı." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#65717E" Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}"/>
            </Grid>
        </Border>
        <Grid Grid.Row="4" Margin="0,9,0,0"><TextBlock Text="{Binding ErrorMessage}" Foreground="#9C342F" VerticalAlignment="Center"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><TextBlock Text="{Binding PageText}" Foreground="#65717E" VerticalAlignment="Center" Margin="0,0,12,0"/><Button Style="{StaticResource Action}" Content="Önceki" Command="{Binding PreviousPageCommand}"/><Button Style="{StaticResource Action}" Content="Sonraki" Command="{Binding NextPageCommand}"/></StackPanel></Grid>

        <Border Grid.RowSpan="5" Background="#F9FFFFFF" BorderBrush="#CBD2D8" BorderThickness="1,0,0,0" Width="440" HorizontalAlignment="Right" Padding="22" Visibility="{Binding IsGrantOpen, Converter={StaticResource BoolToVisibility}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel>
                <Button Content="Kapat" Command="{Binding CloseGrantCommand}" HorizontalAlignment="Right" Style="{StaticResource Action}"/>
                <TextBlock Text="Hızlı Hakediş" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <TextBlock Text="Hedef" Style="{StaticResource Label}"/><ComboBox ItemsSource="{Binding TargetTypes}" DisplayMemberPath="Name" SelectedValuePath="Value" SelectedValue="{Binding TargetType}"/>
                <StackPanel Visibility="{Binding IsManualTarget, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="Öğrenci kimlikleri" Style="{StaticResource Label}"/><TextBox Text="{Binding ManualStudentIds, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="58" ToolTip="Listeden öğrenci seçerek Hızlı Hakediş'i açın veya kimlikleri virgülle girin."/></StackPanel>
                <StackPanel Visibility="{Binding IsClassTarget, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="Sınıf" Style="{StaticResource Label}"/><ComboBox ItemsSource="{Binding Classes}" DisplayMemberPath="Name" SelectedItem="{Binding GrantClass}"/></StackPanel>
                <StackPanel Visibility="{Binding IsGradeTarget, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="Kademe / sınıf seviyesi" Style="{StaticResource Label}"/><TextBox Text="{Binding Grade}" ToolTip="Örnek: 5"/></StackPanel>
                <StackPanel Visibility="{Binding IsGroupTarget, Converter={StaticResource BoolToVisibility}}"><TextBlock Text="Grup" Style="{StaticResource Label}"/><ComboBox ItemsSource="{Binding Groups}" DisplayMemberPath="Name" SelectedItem="{Binding GrantGroup}"/></StackPanel>
                <TextBlock Text="Öğün" Style="{StaticResource Label}"/><ComboBox ItemsSource="{Binding MealTypes}" DisplayMemberPath="Name" SelectedItem="{Binding GrantMeal}"/>
                <TextBlock Text="Tarih aralığı" Style="{StaticResource Label}"/><StackPanel Orientation="Horizontal"><DatePicker Width="150" SelectedDate="{Binding GrantStartsOn}" Margin="0,0,8,0"/><DatePicker Width="150" SelectedDate="{Binding GrantEndsOn}"/></StackPanel>
                <TextBlock Text="Günlük adet (1-10)" Style="{StaticResource Label}"/><TextBox Width="90" HorizontalAlignment="Left" Text="{Binding Quantity}"/>
                <WrapPanel Margin="0,10,0,0"><CheckBox Content="Cumartesi dahil" IsChecked="{Binding IncludeSaturday}" Margin="0,0,18,0"/><CheckBox Content="Pazar dahil" IsChecked="{Binding IncludeSunday}"/></WrapPanel>
                <Separator Margin="0,16"/><Button Content="Etkileri Önizle" Command="{Binding PreviewCommand}" Style="{StaticResource Action}" HorizontalAlignment="Left"/>
                <Border Background="#EDF6F4" Padding="12" Margin="0,12,0,0" Visibility="{Binding HasPreview, Converter={StaticResource BoolToVisibility}}"><StackPanel><TextBlock Text="Önizleme" FontWeight="SemiBold"/><TextBlock Text="{Binding PreviewText}" TextWrapping="Wrap" Margin="0,5,0,10"/><Button Content="Uygula" Command="{Binding ApplyCommand}" Style="{StaticResource Action}" HorizontalAlignment="Left"/></StackPanel></Border>
                <TextBlock Text="{Binding PreviewMessage}" Foreground="#9C342F" TextWrapping="Wrap" Margin="0,10,0,0"/>
            </StackPanel></ScrollViewer>
        </Border>

        <Border Grid.RowSpan="5" Background="#CC17202A" Visibility="{Binding IsCancelConfirmationOpen, Converter={StaticResource BoolToVisibility}}">
            <Border Width="410" Background="White" Padding="24" CornerRadius="7" HorizontalAlignment="Center" VerticalAlignment="Center"><StackPanel><TextBlock Text="Hakları iptal et" FontSize="19" FontWeight="SemiBold"/><TextBlock Text="{Binding CancelConfirmationText}" TextWrapping="Wrap" Margin="0,12,0,18"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="Vazgeç" Command="{Binding CloseCancelCommand}" Style="{StaticResource Action}"/><Button Content="İptali Onayla" Command="{Binding ConfirmCancelCommand}" Style="{StaticResource Action}" Background="#FCEAE8" BorderBrush="#C4483F"/></StackPanel></StackPanel></Border>
        </Border>
        <views:BulkOperationWizardView Grid.RowSpan="5" Panel.ZIndex="20" DataContext="{Binding BulkWizard}"/>
    </Grid>
</UserControl>
